/**
 * Site contact settings (edit here when renting the site to a new client).
 * Lead forms post to FormSubmit using formSubmitEmail.
 * Public "Email us" uses contactMailbox — set DNS forwarding (e.g. Cloudflare) so it reaches you.
 */
window.SITE_CONTACT = {
  formSubmitEmail: "robnattrass429@gmail.com",
  contactMailbox: "contact@omaharoofingexperts.com",
  phone: {
    href: "tel:+12816225095",
    display: "(281) 622-5095",
  },
};
